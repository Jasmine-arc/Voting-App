meta {
  name: ForgotPassword
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/password
  body: json
  auth: inherit
}

body:json {
  {
    "email": "Jasmine@gmail.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
